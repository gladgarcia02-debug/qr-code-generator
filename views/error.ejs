<%- include('partials/header', { title: typeof title !== 'undefined' ? title : 'Error' }) %>

  <section class="error-page">
    <p class="error-page__code"><%= typeof statusCode !== 'undefined' ? statusCode : '' %></p>
    <h1 class="error-page__title"><%= typeof message !== 'undefined' ? message : 'Something went wrong.' %></h1>
    <a href="/" class="btn btn--primary">Back to generator</a>
  </section>

<%- include('partials/footer') %>
